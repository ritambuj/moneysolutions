# Copy this file to `.env.local` and fill in your values.
# Supabase: Project Settings → Database → Connection string (URI)
# Use the "Transaction" pooler URL for serverless, or "Session" for direct connections.

DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.flegvkrzfxsxlrmgfngo.supabase.co:5432/postgres

# Random secret for Payload (generate: openssl rand -hex 32)
PAYLOAD_SECRET=change-me-to-a-long-random-string

# Public URL of your app (local dev)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
